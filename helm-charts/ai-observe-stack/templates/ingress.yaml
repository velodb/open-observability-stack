{{/*
Ingress for AIObserve Stack services
*/}}
{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "aiobs.fullname" . }}
  labels:
    {{- include "aiobs.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "grafana" }}
                name: {{ include "aiobs.grafana.fullname" $ }}
                port:
                  number: {{ $.Values.grafana.service.port }}
                {{- else if eq .service "otel" }}
                name: {{ include "aiobs.otel.fullname" $ }}
                port:
                  number: {{ $.Values.otel.ports.otlpHttp }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
